services:
  - type: web
    name: wavvy
    env: node
    region: oregon
    plan: free
    buildCommand: apt-get update && apt-get install -y yt-dlp && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        fromPort: true
